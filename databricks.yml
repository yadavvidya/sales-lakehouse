bundle:
  name: sales-lakehouse

sync:
  include:
    - dev/src/**/*.py

targets:
  dev:
    workspace:
      profile: dev

include:
  - resources/**/*.yml

permissions:
  - level: CAN_VIEW
    group_name: data_analyst
  - level: CAN_MANAGE
    group_name: data_engineers
  

