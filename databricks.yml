bundle:
  name: sales-lakehouse

include:
  - resources/**/*.yml

sync:
  include:
    - src/**/*.py

targets:
  dev:
    workspace:
      profile: dev



permissions:
  - level: CAN_VIEW
    group_name: data_analyst
  - level: CAN_MANAGE
    group_name: data_engineers
  

